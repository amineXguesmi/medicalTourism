# Shared Types

Shared TypeScript types that are safe to reuse across apps.

Use this package for:

- enums mirrored across clients,
- non-sensitive display types,
- feature flags,
- locale/currency helpers.

Do not put secrets, permission rules or database models here.

